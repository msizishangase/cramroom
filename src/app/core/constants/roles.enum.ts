export enum GroupRole {
  Host = "host",
  Member = "member",
}

export enum EducationStage {
  HighSchool = "high_school",
  Tertiary = "tertiary",
}

export enum FriendshipStatus {
  Pending = "pending",
  Accepted = "accepted",
}
